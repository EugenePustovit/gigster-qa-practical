default: --profile html_report --profile chrome_driver

# reports
html_report: --format html --out report/report.html --format pretty

# drivers
chrome: DRIVER=chrome
firefox: DRIVER=firefox

# driver path
chrome_driver: WEBDRIVER="/usr/local/bin/chromedriver"
